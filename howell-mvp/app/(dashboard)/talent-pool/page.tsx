import TalentPoolClient from '@/components/talent-pool/TalentPoolClient'

// Data is fetched client-side to ensure freshness after portal sync
export default function TalentPoolPage() {
  return <TalentPoolClient />
}
