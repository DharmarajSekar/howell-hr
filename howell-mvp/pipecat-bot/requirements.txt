# Howell HR Bot — HeyGen Avatar Edition
# TTS is now handled by HeyGen on the frontend — no gTTS or ffmpeg needed.

# LiveKit Python SDK — WebRTC room + audio streaming
livekit==0.19.1

# HTTP server (bot launcher)
fastapi==0.111.0
uvicorn[standard]==0.30.0
pydantic==2.7.0

# Async HTTP — Deepgram STT, Gemini LLM
aiohttp==3.9.5

# Audio processing — PCM frame conversion (48kHz → 16kHz downsample)
numpy==1.26.4

# Environment loading
python-dotenv==1.0.1
