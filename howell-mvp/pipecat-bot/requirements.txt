# Pipecat core + required service integrations
# WebRTC transport: LiveKit (open-source, free cloud tier — no credit card)
# NO silero (PyTorch 2GB) — using LiveKit built-in VAD instead
# Pin to 0.x — pipecat 1.x removed OpenAILLMContextAggregator (breaking change)
pipecat-ai[livekit,google,deepgram,elevenlabs]>=0.0.47,<1.0.0

# LiveKit Python SDK (room/token management used by bot)
livekit-api>=0.7.0

# HTTP server (bot launcher)
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0

# Async HTTP (evaluation callback)
aiohttp>=3.9.0

# Environment loading
python-dotenv>=1.0.0
