# Direct LiveKit + AI services implementation (no pipecat)
# LiveKit Python SDK — WebRTC room + audio streaming
livekit==0.19.1

# HTTP server (bot launcher)
fastapi==0.111.0
uvicorn[standard]==0.30.0
pydantic==2.7.0

# Async HTTP — Deepgram STT, Gemini LLM
aiohttp==3.9.5

# Audio processing — reliable frame.data conversion
numpy==1.26.4

# Environment loading
python-dotenv==1.0.1

# Indian English TTS — Google Text-to-Speech (tld='co.in')
gtts==2.5.1
