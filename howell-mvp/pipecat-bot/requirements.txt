# Pipecat core + required service integrations
# Pinned to 0.0.60 — stable, known-good version
# NO silero (PyTorch 2GB) — using Daily built-in VAD instead
# NO webrtcvad — not needed without external VAD
pipecat-ai[daily,google,deepgram,elevenlabs,simli]==0.0.60

# HTTP server (bot launcher)
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0

# Async HTTP (evaluation callback)
aiohttp>=3.9.0

# Environment loading
python-dotenv>=1.0.0
