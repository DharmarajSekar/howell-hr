# Pipecat core + required service integrations
# NOTE: silero removed — requires PyTorch (~2 GB), kills Railway builds
# Using webrtcvad instead (lightweight, no torch dependency)
pipecat-ai[daily,google,deepgram,elevenlabs,simli]>=0.0.47
webrtcvad>=2.0.10

# HTTP server (bot launcher)
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0

# Async HTTP (evaluation callback)
aiohttp>=3.9.0

# Environment loading
python-dotenv>=1.0.0
